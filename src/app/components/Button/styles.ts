import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    container: {
        alignItems: "center",
        paddingTop: 16,
        backgroundColor: "#2c46b1",
        height: 48,
        width: "100%",
        borderRadius: 8,
    },
    title: {
        color: "#FFF",
        fontSize: 14,
        fontWeight: 600,
    },
})