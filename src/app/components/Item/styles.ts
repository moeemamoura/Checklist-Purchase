import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        alignItems: "center",
        flexDirection: "row",
        gap: 7,

    },
    description: {
        flex: 1,
        fontSize: 14,
        fontWeight: 600,
    },
})